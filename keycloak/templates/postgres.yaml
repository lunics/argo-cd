apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name:      keycloak
  namespace: keycloak
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: keycloak
      owner:    keycloak
      secret:
        name: secret-keycloak-login
